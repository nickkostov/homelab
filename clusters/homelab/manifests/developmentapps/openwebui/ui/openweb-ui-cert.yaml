apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openwebui-tls
  namespace: openwebui
spec:
  secretName: openwebui-tls
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - ai.range.local
    - openwebui.range.local  # Add any additional hostnames you want
